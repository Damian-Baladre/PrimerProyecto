const iconoMenu = getElementById();
const cerrarMenu = getElementById();
const navigator = getElementById('navigatorList');

IconMenu.addEventlistener('click', () => {
    nav.classList(open)
})