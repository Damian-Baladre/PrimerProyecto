//Concatenar cadenas de un array: Crea un array de cadenas de texto y utiliza for...of para concatenarlas en una sola cadena separada por espacios.

/*const textos = [
  "hola",
  "cara",
  "de",
  "pito",
  "allá",
  "la",
  "estan",
  "piteando",
];

let resultado = "";

for (let texto of textos) {
  resultado = texto += " ";
}

resultado = resultado.trim();
*/

/*Contar propiedades de un objeto:
Usa for...in para contar cuántas propiedades tiene un objeto que representa las configuraciones de una aplicación.*/

/*const propiedades = {
  theme: "dark",
  language: "es",
  notifications: true,
  version: "1.0.0",
  autoSave: false,
};

let contador = "";

for (let configuraciones in propiedades) {
  contador++;
}

console.log(contador);*/

/*Recorrer un array bidimensional: Tienes un array de arrays (matriz) de números.
 Utiliza bucles for...of anidados para imprimir todos los elementos de la matriz.*/

/*const matriz = [
  [20, 30, 25],
  [65, 68, 55],
  [34, 52, 57],
  [93, 12, 99],
];

for (fila of matriz) {
  for (numeros of fila) {
    console.log(numeros);
  }
};

Diferenciar métodos y propiedades en un objeto: Dado un objeto que contiene propiedades y métodos,
utiliza for...in para iterar sobre él e imprimir solo las propiedades (excluyendo los métodos).*/
